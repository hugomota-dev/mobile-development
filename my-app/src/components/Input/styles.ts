import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  inputGroup: {
    width: "100%",
    backgroundColor: "#fff",
    flexDirection: "row",
    alignItems: "center",
    paddingLeft: 16,
    borderRadius: 10,
    borderColor: "#757575",
    borderBottomWidth: 1,
    overflow: "hidden",
    
  },
  inputText: {
    paddingLeft: 12,
  },
});

import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    // backgroundColor: "black",
    alignItems: "center",
    padding: 60,
  },

  loginBox: {
    width: "100%",
    gap: 10,
    alignItems: "center",
  },

  wellcome: {
    paddingBottom: 18,
    fontSize: 20,
    fontWeight: "bold",
  },

  buttonBox: {
    width: "100%",
    alignItems: "flex-end",
  },
});
